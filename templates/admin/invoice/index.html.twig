{% extends 'base.html.twig' %}

{% block title %}{% trans %}Invoice index{% endtrans %}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/main.css') }}">
{% endblock %}

{% block body %}
    <h1>{% trans %}Invoice index{% endtrans %}</h1>
    <a href="{{ path('admin_invoice_new') }}" class="btn btn-outline-success mb-2">{% trans %}Create new{% endtrans %}</a>
    <table class="table invoice">
        <thead>
            <tr>
                <th><span class="d-md-inline d-none">{% trans %}Customer{% endtrans %}</span></th>
                <th>{% trans %}Date{% endtrans %}</th>
                <th>{% trans %}Sold{% endtrans %}</th>
                <th></th>
                <th><i class="bi bi-archive"></i></th>
                <th><i class="bi bi-printer"></i></th>
                <th><i class="bi bi-envelope-open"></i></th>
                <th class="d-md-table-cell d-none">{% trans %}Email{% endtrans %}</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for invoice in invoices %}
            <tr class="invoice-row">
                <td><span>{{ invoice.Customer.id }}</span><span class="d-md-inline d-none"> - {{  invoice.Customer.name }}</span></td>
                <td>{{ invoice.createdAt|date('d/m/Y') }}</td>
                <td class="invoice-sold">
                    {{ invoice.sold|number_format(2) }}
                </td>
                <td>
                    <div class="d-inline-block justify-content-end">
                        {% if invoice.sold > 0 %}
                            <button type="button" class="btn btn-link newPayment" data-bs-toggle="modal" data-bs-target="#newPayment" data-id-invoice="{{ invoice.id }}" data-sold="{{ invoice.sold }}">
                                <i class="bi bi-cash-coin"></i>
                            </button>
                        {% endif %}
                        <button type="button" class="btn btn-link paymentList text-primary" data-bs-toggle="modal" data-bs-target="#paymentList-{{ invoice.id }}">
                            <i class="bi bi-cash-stack"></i>
                        </button>
                    </div>
                </td>
                <td>{{ invoice.archived ? '<i class="bi bi-check-circle text-success"></i>' : '<i class="bi bi-x-circle text-danger"></i>' }}</td>
                <td>{{ invoice.printed ? '<i class="bi bi-check-circle text-success"></i>' : '<i class="bi bi-x-circle text-danger"></i>' }}</td>
                <td>{{ invoice.sendByMail ? '<i class="bi bi-check-circle"></i>' : '<i class="bi bi-x-circle"></i>' }}</td>
                <td class="d-md-table-cell d-none">{{ invoice.email }}</td>
                <td>
                    {% if invoice.archived %}
                        <form method="post" action="{{ path('admin_invoice_unarchive', {'id': invoice.id}) }}" onsubmit="return confirm('{% trans %}Are you sure you want to unarchive this item?{% endtrans %}');" class="form-inline d-inline">
                            <input type="hidden" name="_token" value="{{ csrf_token('unarchive' ~ invoice.id) }}">
                            <button class="btn btn-outline-secondary"><i class="bi bi-box-arrow-in-up"></i><span class="d-md-inline d-none"> {% trans %}unarchive{% endtrans %}</span></button>
                        </form>
                    {% else %}
                        <form method="post" action="{{ path('admin_invoice_archive', {'id': invoice.id}) }}" onsubmit="return confirm('{% trans %}Are you sure you want to archive this item?{% endtrans %}');" class="form-inline d-inline">
                            <input type="hidden" name="_token" value="{{ csrf_token('archive' ~ invoice.id) }}">
                            <button class="btn btn-outline-secondary"><i class="bi bi-box-arrow-in-down"></i><span class="d-md-inline d-none"> {% trans %}archive{% endtrans %}</span></button>
                        </form>
                    {% endif %}
                    <a href="{{ path('admin_invoice_edit', {'id': invoice.id}) }}" class="btn btn-primary"><i class="bi bi-pencil"></i><span class="d-md-inline d-none"> {% trans %}edit{% endtrans %}</span></a>

                    <!-- Modal payment list -->
                    <div class="modal fade" id="paymentList-{{ invoice.id }}" tabindex="-1" aria-labelledby="paymentListModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="paymentListModalLabel">{% trans %}Payment list{% endtrans %}</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{% trans %}Close{% endtrans %}"></button>
                                </div>
                                <div class="modal-body">
                                    {%  if invoice.payments is not empty %}
                                        <table class="table table-striped table-sm">
                                            <thead>
                                            <tr>
                                                <th>{% trans %}Amount{% endtrans %}</th>
                                                <th>{% trans %}Date{% endtrans %}</th>
                                                <th>{% trans %}Mode{% endtrans %}</th>
                                                <th>{% trans %}Action{% endtrans %}</th>
                                            </tr>
                                            </thead>
                                            {% for payment in invoice.payments %}

                                                <tr>
                                                    <td>{{ payment.amount }}</td>
                                                    <td>{{ payment.date|date("d/m/Y") }}</td>
                                                    <td>{{ payment.mode == 0 ? 'Money'|trans }}{{ payment.mode == 1 ? 'Check'|trans }}{{ payment.mode == 2 ? 'Bank transfert'|trans }}{{ payment.mode == 3 ? 'Bank card'|trans }}{{ payment.mode == 4 ? 'Paypal'|trans }}{{ payment.mode == 5 ? 'Other'|trans }}</td>
                                                    <td class="text-right">
                                                        <a href="#" class="btn btn-danger delete-payment" data-delete-token="{{ csrf_token('delete' ~ payment.id) }}" data-delete-url="{{ path('admin_payment_delete', {'id': payment.id}) }}"><i class="bi bi-trash"></i></a>

                                                    </td>
                                                </tr>
                                            {% endfor %}

                                        </table>
                                    {% else %}
                                        {% trans %}This invoice have't any payment{% endtrans %}
                                    {% endif %}
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">{% trans %}Close{% endtrans %}</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">{% trans %}no records found{% endtrans %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_invoice_new') }}" class="btn btn-outline-success">{% trans %}Create new{% endtrans %}</a>
    {{ include('admin/invoice/_modal.html.twig') }}
{% endblock %}
